<Window x:Class="SonoCap.MES.UI.Views.FirstView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SonoCap.MES.UI.Views"
        xmlns:Converter="clr-namespace:SonoCap.MES.UI.Converters"
        xmlns:Behavior="clr-namespace:SonoCap.MES.UI.Behaviors"
        xmlns:vm="clr-namespace:SonoCap.MES.UI.ViewModels"
        WindowState="Maximized"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type={x:Type vm:FirstViewModel}}" Title="{Binding Title}" Height="1080" Width="1920">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="100"/>
        </Grid.RowDefinitions>
        <Image Grid.Row="1" Source="{Binding Logo}"/>
        <StackPanel Grid.Row="1" Orientation="Vertical" VerticalAlignment="Bottom">
            <StackPanel Height="50" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Add MasterData" FontSize="15" Margin="5" IsDefault="True" Command="{Binding AddMasterDataCommand}" Visibility="Hidden" />
                <Button Content="Import TD Excel" FontSize="15" Margin="5" IsDefault="True" Command="{Binding ImportTDExcelCommand}" Visibility="Hidden" />
                <Button Content="Import MT Excel" FontSize="15" Margin="5" IsDefault="True" Command="{Binding ImportMTExcelCommand}" Visibility="Hidden" />
            </StackPanel>
            <StackPanel Height="50" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Testing" FontSize="15" Margin="5" IsDefault="True" Command="{Binding GoTestingViewCommand}"  Visibility="Hidden" />
                <Button Content="Tests" FontSize="15" Margin="5" IsDefault="True" Command="{Binding GoTestListViewCommand}"  Visibility="Hidden" />
                <Button Content="Probes" FontSize="15" Margin="5" IsDefault="True" Command="{Binding GoProbeListViewCommand}"  Visibility="Hidden" />
                <Button Content="Preview" FontSize="15" Margin="5" IsDefault="True" Command="{Binding GoPreviewSaveViewCommand}"  Visibility="Visible" />
            </StackPanel>
        </StackPanel>
        
    </Grid>
</Window>
