<Application x:Class="MES.UI.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:MES.UI" 
             xmlns:convertes="clr-namespace:MES.UI.Converters"
             xmlns:Behaviors="clr-namespace:MES.UI.Behaviors">
    <Application.Resources>
        <convertes:RadioButtonIndexConverter x:Key="RadioButtonIndexConverter" />
        <convertes:RowIndexConverter x:Key="RowIndexConverter" />
        <Behaviors:DataGridBehavior x:Key="DataGridBehavior" />
        <!--<ResourceDictionary>
            <vm:ViewModelLocator
                xmlns:vm="clr-namespace:UI.Test.ViewModels"
                x:Key="Locator"
                d:IsDataSource="True" />

            <Style TargetType="Window">
                <Setter Property="FontSize" Value="28" />
            </Style>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/BusyIndicator;component/Theme/Default.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>-->
        
    </Application.Resources>
</Application>
